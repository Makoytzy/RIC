import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function ProductRelease() {
  return (
    <PlaceholderPage
      title="Product Release"
      description="Release sold products to the customer at pickup or dispatch."
      tag="Sales Staff"
      actions={["Release product", "View pending releases"]}
    />
  );
}
