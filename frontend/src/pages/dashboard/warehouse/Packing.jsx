import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function Packing() {
  return (
    <PlaceholderPage
      title="Packing"
      description="Pack picked items for outgoing shipment."
      tag="Warehouse Staff"
      actions={["Start packing", "Mark packed"]}
    />
  );
}
