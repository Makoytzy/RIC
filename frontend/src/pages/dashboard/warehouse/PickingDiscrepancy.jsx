import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function PickingDiscrepancy() {
  return (
    <PlaceholderPage
      title="Picking Discrepancy"
      description="Report and resolve mismatches found during picking."
      tag="Warehouse Staff"
      actions={["Report discrepancy", "View open cases"]}
    />
  );
}
