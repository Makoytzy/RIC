import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function RefundReports() {
  return (
    <PlaceholderPage
      title="Refund Reports"
      description="Refund totals, methods, and approval turnaround time."
      tag="Manager"
      actions={["Generate report", "Export CSV"]}
    />
  );
}
