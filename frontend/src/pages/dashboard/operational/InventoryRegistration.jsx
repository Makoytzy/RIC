import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function InventoryRegistration() {
  return (
    <PlaceholderPage
      title="Inventory Registration"
      description="Register newly received stock into the inventory system."
      tag="Operational Staff"
      actions={["Register stock", "View recent entries"]}
    />
  );
}
