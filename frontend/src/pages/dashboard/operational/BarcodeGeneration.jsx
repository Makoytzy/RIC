import PlaceholderPage from '../../../components/common/PlaceholderPage.jsx';

export default function BarcodeGeneration() {
  return (
    <PlaceholderPage
      title="Barcode Generation"
      description="Generate and print barcodes for products and batches."
      tag="Operational Staff"
      actions={["Generate barcode", "Print labels"]}
    />
  );
}
